bool anagram(string s, string t)
{
	if(s.length() != t.length()) return false;
	int letters[256] = {0};
	for(int i = 0; i < s.length(); ++i) ++letters[s[i]];
	for(int i = 0; i < t.length(); ++i){
		if(letters[t[i]] == 0) return false;
		--letters[t[i]];
	}
	return true;
}
